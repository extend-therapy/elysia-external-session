{
  "name": "@extend-therapy/elysia-external-session",
  "version": "0.0.16",
  "description": "Elysia External Session",
  "author": "Eli Selkin",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/extend-therapy/elysia-external-session.git"
  },
  "publishConfig": {
    "@extend-therapy:registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "build": "rm -rf dist || true && tsc",
    "prepublish": "bun lint && bun test && bun run build",
    "test": "bun test",
    "dev": "tsc --watch",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "pub": "op run --env-file=.env.npm -- bun publish"
  },
  "main": "dist/src/index.js",
  "type": "module",
  "types": "./dist/src/index.d.ts",
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "dotenv-cli": "^11.0.0",
    "elysia": "^1.4.22",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0",
    "oxlint-tsgolint": "^0.11.1",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "date-fns": "^4.1.0",
    "elysia": "^1.4.15"
  },
  "engines": {
    "bun": ">=1.3.3"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}